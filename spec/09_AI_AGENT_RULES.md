# AI Agent Execution Contract
`/spec` is authoritative.

Never invent clients, awards, testimonials, project metrics, services, contact data, social URLs, YouTube IDs, thumbnails, analytics or admin credentials. Unknown content stays empty/configurable. Runtime fake projects are prohibited; fixtures belong only in tests/demo stories.

Never render raw admin-provided iframe/HTML. Validate YouTube URLs and construct embeds from trusted parsed IDs. Client-side auth is never security.

Do not drift into generic SaaS aesthetics; read `02_VISUAL_IDENTITY.md` before public UI tasks.

Execution mode: finish one phase at a time with internal `IMPLEMENT -> TEST -> VERIFY -> CORRECT` gates, then stop at the phase boundary or genuine blocker.
