# Content Editing Rules
Public editorial copy comes from persisted content. EN and AR are independently editable. Structural layout stays code-controlled; owner edits safe structured fields. No arbitrary scripts/iframe/raw HTML. YouTube URLs go through the media parser. Images require alt text. Draft projects never render publicly.
